import styled from 'styled-components/native';

  export const InputContainer = styled.View`
    marginLeft: 10px;
    flexDirection: row;
    alignItems: center;
    justifyContent: center;
  `

  export const SendPostPic = styled.Image`
    width: 35px;
    height: 35px;
    borderRadius: 5px;
  `

  // export const SearchContainer = styled.View`
  //   borderRadius: 10px;
  //   backgroundColor: #000;
  // `

  export const Search = styled.View`
    width: 15px;
    height: 15px;
    marginLeft: 10px;
    marginRight: 15px;

  `
    
  export const CreateGroupContainer = styled.TouchableOpacity`
    flexDirection: row;
    justifyContent: space-between;
    alignItems: center;
    marginBottom: 10px
  `

  export const CreateGroupIcon = styled.View`
    height: 35px;
    width: 35px;
    borderRadius: 100%;
  `

  export const CreateGroupText = styled.Text`
    color: #fff;
    fontWeight: bold;

  `

  export const CreateGroupTextContainer = styled.View`
    marginRight: auto;
    paddingLeft: 10px
  `

  export const SendStoryContainer = styled.View`
    flexDirection: row;
    alignItems: center;
    marginBottom: 10
  `

  export const ProfilePic = styled.Image`
    height: 35px;
    width: 35px;
    borderRadius: 100%;
  `

  export const SendStoryTextContainer = styled.TouchableOpacity`
    paddingLeft: 10px;
  `

  export const SendStoryText = styled.Text`
    color: #0095f6
  `
  export const SearchContainer = styled.View`
    flexDirection: row;
    marginBottom: 10;
    borderRadius: 5px;
    backgroundColor: #494949;
    paddingTop: 5px;
    paddingBottom: 5px;
  `

  export const SendWithTextContainer = styled.View`
    flexDirection: row;
    marginBottom: 20
  `
