import styled from 'styled-components/native';
 
import {Dimensions} from "react-native";

let width = Dimensions.get('window').width;


styledButton = styled.Image`
  position:"absolute",
  top: 20,
  zIndex: 200
`

styledImage = styled.Button`
  height: '75px',
  width: '75px'
`


  